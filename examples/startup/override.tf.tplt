terraform {
  required_providers {
    azurerm = {
      source  = "hashicorp/azurerm"
      version = "{{ env "TERRAFORM_AZURERM_VERSION" | default ">= 2.90.0, < 3.0.0"}}"
    }
  }
}

module "aks" {
  source = "{{env "MODULE_SOURCE" |  default "../../"}}"
}